/**
 * `@schemic/surrealdb/query` subpath entry — the SurrealDB-owned typed query builder (opt-in,
 * tree-shakeable; a schema-only project never pulls it). See `./query/index`.
 */
export * from "./query/index";
